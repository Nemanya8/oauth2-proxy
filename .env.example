# OAuth2 Proxy Environment Variables

# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
OAUTH2_PROXY_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com
OAUTH2_PROXY_CLIENT_SECRET=YOUR_CLIENT_SECRET

# Cookie Secret (generate with: python -c 'import os,base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())')
# Must be base64url encoded 32 bytes (will be 44 characters with padding)
OAUTH2_PROXY_COOKIE_SECRET=YOUR_GENERATED_COOKIE_SECRET

# Redirect URL (update with your actual domain)
# For local testing: http://localhost:4180/oauth2/callback
# For production: https://your-domain.com/oauth2/callback
OAUTH2_PROXY_REDIRECT_URL=http://localhost:4180/oauth2/callback
